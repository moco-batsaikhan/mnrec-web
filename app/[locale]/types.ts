export type Locale = "en" | "mn";

export type Dictionary = {
  home: {
    edit: string;
    save: string;
  };
  cta: {
    deploy: string;
    docs: string;
  };
  links: {
    learn: string;
    examples: string;
    next: string;
  };
};
